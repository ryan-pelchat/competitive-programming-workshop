    # newWord = ""
    # while newWord in usedWords:
    #     newWord = ""
    #     for i in range(random.randint(2, 14)):
    #         newWord += chr(random.randint(97, 122))

    # usedWords.add(newWord)
    # output.append(newWord)